const { calculateBudget } = require("./budget");
const { calculateCurtain } = require("./curtain");
const { calculateFlooring } = require("./flooring");
const { calculateGrout } = require("./grout");
const { calculatePaint } = require("./paint");
const { calculateTile } = require("./tile");

module.exports = {
  calculateBudget,
  calculateCurtain,
  calculateFlooring,
  calculateGrout,
  calculatePaint,
  calculateTile
};
